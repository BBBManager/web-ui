targetBackend = 'json';

$(document).ready(function() {
	//initDataTables();
	
	
	/*
	fetchRoom = function(roomId){
	    $.ajax({
	        url         : '/api/rooms/' + roomId + '.' + targetBackend,
	        method      : 'get',
	        dataType    : targetBackend,
	        success     : function(a) {
	            room = a.retorno.room;
	            for(atr in room) {
	                $('#' + atr).val(room[atr]);
	            }
	        }
	    });
	}
	*/
	
	
    
});
